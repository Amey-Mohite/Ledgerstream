# AI Query service — runtime dependencies (FastAPI, not Django).
fastapi>=0.115
uvicorn[standard]>=0.30              # ASGI server
pyjwt>=2.9                           # stateless JWT validation (shared key)
httpx>=0.27                          # calls the Ledger read API (tenant-scoped tools)
python-dotenv>=1.0

# LLM providers (multi-provider gateway; used when their key is set)
anthropic>=0.40
openai>=1.50

# MCP server (exposes the ledger read tools over the Model Context Protocol)
mcp>=1.2

# Shared observability library (installed separately, editable):
#   pip install -e ../../libs/shared
