# Payment service — runtime dependencies
Django>=5.1,<5.2
djangorestframework>=3.15
djangorestframework-simplejwt>=5.3        # JWT auth for DRF
psycopg[binary]>=3.2                       # PostgreSQL driver (psycopg3)
dj-database-url>=2.2                        # parse DATABASE_URL (Neon) into Django config
gunicorn>=22.0                             # production WSGI server
python-dotenv>=1.0                         # load .env for native local dev

# Shared observability library (installed separately, editable):
#   pip install -e ../../libs/shared
