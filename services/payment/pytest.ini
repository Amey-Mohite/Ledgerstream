[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = test_*.py
; --reuse-db keeps the test database between runs (fast, and avoids the
; DROP-DATABASE-at-teardown conflict with persistent connections). Use
; `--create-db` once to rebuild it after a schema/migration change.
addopts = -q --reuse-db
